repoName = 'Exampled'
projName = 'Main'

platform :ios, '9.0'

workspace repoName

use_frameworks!

#===

def sharedPods

	pod 'XCEUniFlow', '~> 4.6'
	pod 'XCEModelDependable', '~> 1.0'

end

#===

target 'Fwk' do

	project projName

	#===

	sharedPods

end
